<%= render 'shared/errors', obj: @category %>
<%= form_with(model: @category, class: "shadow p-3 mb-3 bg-info rounded", local: true) do |f| %>
  <div class="form-group row">
    <%= f.label :name, "Nome da Categoria", class: "col-2 col-form-label text-light" %>
    <div class="col-10">
      <%= f.text_field :name, class: "form-control shadow rounded", placeholder: "Entre com a categoria" %>
    </div>
  </div>

  <div class="form-group row justify-content-center">
    <%= f.submit "Criar categoria", class: "btn btn-outline-light btn-lg" %>
  </div>
<% end %>